{
    "name": "{{ config.APP_NAME }}",
    "start_url": "{{ url_for('index') }}",
    "display": "standalone",
    "icons": [
        {
            "src": "{{ url_for('static', filename='icon.png') }}",
            "type": "image/png",
            "sizes": "768x768"
        }
    ]
}